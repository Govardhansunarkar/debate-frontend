# Production PeerJS Configuration
# For single-port deployment, use a reverse proxy (nginx/Apache) to route /peerjs requests to the PeerJS server port
# Example nginx configuration to proxy /peerjs to port 9000 while other requests go to port 8000
VITE_PEERJS_HOST=your-domain.com
VITE_PEERJS_PORT=443
